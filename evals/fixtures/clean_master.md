# Clean master fixture (合成乾淨母本，供 run_evals 回歸測試)

This is a synthetic clean master used by the eval harness. Its sentences are
deliberately simple so that verbatim quotes grep back deterministically.

Burke draws on the address to the electors of Bristol as the trustee pole.

The author applies the principle of relative independence of representatives.

However, the recall may serve as a form of vertical control in some systems.

Madison offers an elitist understanding of representation that the paper rejects.

Only if the electorate is well informed does the recall improve representation.

This sentence contains no special markers and stays purely descriptive.
